name: Full Pipeline (Server + Client + Sonar)

on:
  push:
    branches:
      - dev
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test_and_scan:
    runs-on: ubuntu-latest
    steps:
      # 1) CHECKOUT CODICE
      - name: Checkout code
        uses: actions/checkout@v3

      # 2) SETUP PYTHON & DEPENDENZE
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r server/envs/requirements.txt
          pip install pytest
          pip install selenium

      - name: Create virtual environments if not exist
        run: |
          python -m venv server/envs/domain_prediction_env
          python -m venv server/envs/tasks_prediction_env
          python -m venv server/envs/file_management_env
          python -m venv server/envs/report_generation_env
          python -m venv server/envs/feedback_env
          python -m venv server/envs/api_gateway_env

      # 3) AVVIO DEI MICROSERVIZI E API GATEWAY
      - name: Start microservices and API Gateway
        run: |
          source server/envs/domain_prediction_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/microservices/domain_prediction_service.py &
          sleep 2
          
          source server/envs/tasks_prediction_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/microservices/task_prediction_service.py &
          sleep 2
          
          source server/envs/file_management_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/microservices/file_management_service.py &
          sleep 2
          
          source server/envs/report_generation_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/microservices/report_generation_service.py &
          sleep 2
          
          source server/envs/feedback_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/microservices/feedback_service.py &
          sleep 2
          
          source server/envs/api_gateway_env/bin/activate && pip install -r server/envs/requirements.txt
          nohup python server/api_gateway/api_gateway.py &
          sleep 2

      # 4) SETUP NODE + AVVIO CLIENT
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies (Client)
        run: npm install
        working-directory: ./client/web_app

      - name: Start client application
        run: |
          nohup npm run dev --prefix ./client/web_app &
          sleep 5

      - name: Check if client is running
        run: curl -I http://localhost:5173 || exit 1

      # 5) INSTALLA FIREFOX
      - name: Install Firefox and Geckodriver
        run: |
          sudo apt-get update
          sudo apt-get install -y firefox
          GECKODRIVER_VERSION=$(curl -s https://api.github.com/repos/mozilla/geckodriver/releases/latest | grep 'tag_name' | cut -d '"' -f 4)
          wget "https://github.com/mozilla/geckodriver/releases/download/$GECKODRIVER_VERSION/geckodriver-$GECKODRIVER_VERSION-linux64.tar.gz"
          tar -xvzf geckodriver-$GECKODRIVER_VERSION-linux64.tar.gz
          sudo mv geckodriver /usr/local/bin/
          geckodriver --version
          

        # 5) TEST E SCANSIONE SONARCLOUD
      - name: Run Pytest
        env:
          DISPLAY: :99
        run: |
          source server/envs/api_gateway_env/bin/activate
          pytest system_tests/tests --maxfail=1 --disable-warnings -q

      - name: Client test and coverage
        run: npm run coverage
        working-directory: ./client/web_app

      - name: Setup Java (for SonarCloud)
        uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: temurin

      # - name: SonarCloud Scan (Server)
      #  uses: SonarSource/sonarcloud-github-action@master
      #  env:
      #    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_SERVER }}
      #  with:
      #    projectBaseDir: refair-server/

      # - name: SonarCloud Scan (Client)
      #  uses: SonarSource/sonarcloud-github-action@master
      #  env:
      #    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_CLIENT }}
      #  with:
      #    projectBaseDir: client/web_app/
